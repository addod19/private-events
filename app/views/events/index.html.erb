<div class="container">
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-7 center jumbotron">
            <%= render 'list_all_events'%>
        </div>
        <div class="col-md-2"></div>
    </div>
</div>

<hr>

<h3>Upcoming Events</h3>
<% if @upcoming.any? %>
    <ul>
        <%= render @upcoming %>

    </ul>
<% end %>

<hr>
<h3>Past Events</h3>
<% if @past.any? %>
    <ul>
        <%= render @past %>

    </ul>
<% end %>